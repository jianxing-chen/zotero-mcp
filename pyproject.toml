[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "zotero-mcp-server"
dynamic = ["version"]
authors = [
  { name = "54yyyu", email = "54yyyu@github.com" },
]
description = "A Model Context Protocol server for Zotero"
keywords = ["zotero", "mcp", "model-context-protocol", "cli", "research", "citations", "bibliography"]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "pyzotero>=1.8.0",
    "mcp>=1.2.0",
    "python-dotenv>=1.0.0",
    "markitdown[pdf]",
    "pydantic>=2.0.0",
    "requests>=2.28.0",
    "fastmcp>=2.14.0",
    "unidecode>=1.3.0",
    "bibtexparser>=1.4,<2",
]

[project.optional-dependencies]
semantic = [
    # >=1.0.0 for chromadb.utils.embedding_functions.register_embedding_function,
    # used to register the custom OpenAI/Gemini/HF embedding functions (#315).
    "chromadb>=1.0.0",
    "sentence-transformers>=2.2.0",
    "openai>=1.30.0",
    "google-genai>=0.7.0",
    "tiktoken>=0.7.0",
]
pdf = [
    # >=1.24.2 for Page.cluster_drawings (vector figure detection in zotero_get_page_layout)
    "pymupdf>=1.24.2",
    "ebooklib>=0.18",
]
scite = [
    "requests>=2.28.0",
]
mineru = [
    # Structured PDF parsing (formulas as LaTeX, tables as HTML) for the
    # zotero_read_pdf_pages tool. Only needed for local backends (hybrid/pipeline);
    # the ``api`` backend needs just ``requests`` (already a core dep). Heavy:
    # pulls torch/ray. Install in a separate venv and point ``mineru.executable``
    # at the CLI to keep this package's dep tree light.
    "mineru[all]",
    "requests>=2.28.0",
]
browser = [
    # Browser-session publisher PDF fetcher for zotero_upgrade_preprint_pdfs_via_browser.
    # Connects to a live Chrome/Edge DevTools session (--remote-debugging-port)
    # to download paywalled publisher PDFs that are blocked by WAFs/captchas
    # when fetched via plain requests.get. Only needed for the browser-fallback
    # tool; the HTTP-only upgrade_preprint_pdfs does not require this.
    "websocket-client>=1.8.0",
]
all = [
    "zotero-mcp-server[semantic,pdf,scite]",
]
dev = [
    "zotero-mcp-server[all]",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21",
    "pytest-timeout>=2.0.0",
    "ruff>=0.9.0",
]

[project.urls]
"Homepage" = "https://github.com/54yyyu/zotero-mcp"
"Bug Tracker" = "https://github.com/54yyyu/zotero-mcp/issues"
"Documentation" = "https://stevenyuyy.com/zotero-mcp/"
"Changelog" = "https://github.com/54yyyu/zotero-mcp/blob/main/CHANGELOG.md"

[project.scripts]
zotero-cli = "zotero_mcp.cli_standalone:main"
zotero-mcp = "zotero_mcp.cli:main"


[tool.hatch.version]
path = "src/zotero_mcp/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/zotero_mcp"]

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
ignore = ["E501"]

[tool.pytest.ini_options]
timeout = 30
testpaths = ["tests"]
# auto mode lets async tests run without per-test @pytest.mark.asyncio,
# and silences PytestUnknownMarkWarning for existing decorated tests.
asyncio_mode = "auto"
filterwarnings = [
    "ignore::DeprecationWarning",
]
